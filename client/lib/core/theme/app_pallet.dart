import 'package:flutter/material.dart';

class Pallete {
  static const cardColor = Color.fromRGBO(24, 24, 24, 1); // Dark card background
  static const greenColor = Color(0xFF1DB954); // Spotify green
  static const subtitleText = Color(0xFFB3B3B3); // Light grey for text
  static const inactiveBottomBarItemColor = Color(0xFF535353); // Dark grey for inactive items

  static const Color backgroundColor = Color.fromRGBO(18, 18, 18, 1); // Main background
  static const Color gradient1 = Color.fromRGBO(30, 215, 96, 1); // Spotify green gradient start
  static const Color gradient2 = Color.fromRGBO(80, 220, 100, 1); // Slightly lighter green
  static const Color gradient3 = Color.fromRGBO(120, 220, 110, 1); // Another variation
  static const Color borderColor = Color.fromRGBO(40, 40, 40, 1); // Border to match dark theme
  static const Color whiteColor = Colors.white; // Standard white for text or icons
  static const Color greyColor = Color(0xFFB3B3B3); // Light grey for secondary text
  static const Color errorColor = Colors.redAccent; // Standard error color
  static const Color transparentColor = Colors.transparent; // Transparent for overlays

  static const Color inactiveSeekColor = Colors.white38; // For seek bar inactive portion
}
